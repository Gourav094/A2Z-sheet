Approach:
Just take two pointer at first and last
store the sum of both pointer data
if target is greater equals to data then move first=first->next and store in vector 
if target < sum ==> then move last=last->prev
CODE :
class Solution
{
public:
    vector<pair<int, int>> findPairsWithGivenSum(Node *head, int target)
    {
        // code here
        vector<pair<int,int>>ans;
        Node *first=head;
        Node* last=head;
        while(last->next != NULL){
            last=last->next;
        }
        while(first != NULL && last != first){
            int sum=first->data + last->data;
            if(target > sum){
                first=first->next;
            }
            else if(sum == target){
                ans.push_back({first->data,last->data});
                first=first->next;

            }
            else last=last->prev;
        }
        return ans;
    }
};
